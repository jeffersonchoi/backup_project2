<h1> <%= @user.first_name %> </h1>

<table>
	<tr>
		<th> First Name </th>
		<th> Last Name </th>
    <th> Email </th>
		<th> Items Details</th>
	</tr>

		<tr>
			<td> <%= @user.first_name %> </td>
			<td> <%= @user.last_name %> </td>
      <td> <%= @user.email %> </td>
      <td> <%= link_to "#{@user.first_name}'s Items List", user_items_path(@user) %>
		</tr>
</table>
<%= link_to "Back to Users' Lists", users_path %>
